export function backpropagate(node: any, result: number): void {
    // Mocked backpropagation logic
    console.log("Backpropagating result:", result, "for node:", node);
    // Replace this with logic to update the node's statistics
  }
  